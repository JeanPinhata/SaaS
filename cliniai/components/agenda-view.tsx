"use client";
import { useActionState, useEffect, useState } from "react";
import { CalendarPlus, Check, Clock3, X } from "lucide-react";
import { useRouter } from "next/navigation";
import { createAppointmentAction, setAppointmentStatusAction, type AgendaState } from "@/app/agenda/actions";
import type { DemoAppointment, Patient, Professional, Room, Service } from "@/lib/types";
const initial: AgendaState = {};
const clock = new Intl.DateTimeFormat("pt-BR", { hour:"2-digit", minute:"2-digit", timeZone:"America/Sao_Paulo" });
export function AgendaView({ appointments, patients, professionals, services, rooms, date }: { appointments: DemoAppointment[]; patients: Patient[]; professionals: Professional[]; services: Service[]; rooms: Room[]; date:string }) { const [open,setOpen]=useState(false); const router=useRouter(); const [state,action,pending]=useActionState(createAppointmentAction,initial); useEffect(()=>{if(state.success)router.refresh()},[router,state.success]); return <main className="directory-content"><header className="directory-header"><div><p className="eyebrow blue">AGENDA</p><h1>Agenda da clínica</h1><p>Horários protegidos contra conflito de profissional e sala.</p></div><button className="primary-button" onClick={()=>setOpen(true)}><CalendarPlus size={16}/> Agendar consulta</button></header><section className="agenda-toolbar"><form><input aria-label="Data da agenda" type="date" name="date" defaultValue={date}/><button>Ver data</button></form><span><Clock3 size={15}/> {appointments.length} consultas neste dia</span></section><section className="agenda-list">{appointments.map((appointment)=><article className="agenda-item" key={appointment.id}><time>{clock.format(new Date(appointment.startsAt))}</time><div className="agenda-line"/><div><strong>{appointment.patientName}</strong><p>{appointment.professionalName} · {appointment.roomId === "room_1" ? "Consultório 1" : appointment.roomId === "room_2" ? "Consultório 2" : "Sala de procedimento"}</p></div><em className={appointment.status === "CONFIRMED" ? "status confirmed" : appointment.status === "WAITING_CONFIRMATION" ? "status waiting" : "status scheduled"}>{appointment.status === "CONFIRMED" ? "Confirmado" : appointment.status === "WAITING_CONFIRMATION" ? "Aguardando" : appointment.status === "SCHEDULED" ? "Agendado" : appointment.status === "CANCELLED" ? "Cancelado" : appointment.status}</em><span className="agenda-actions">{appointment.status === "SCHEDULED" ? <button onClick={()=>setAppointmentStatusAction(appointment.id,"CONFIRMED").then(()=>router.refresh())}><Check size={15}/> Confirmar</button> : null}{!["CANCELLED","COMPLETED","NO_SHOW"].includes(appointment.status) ? <button className="danger-action" onClick={()=>setAppointmentStatusAction(appointment.id,"CANCELLED").then(()=>router.refresh())}>Cancelar</button> : null}</span></article>)}</section>{open?<div className="sheet-backdrop" role="dialog" aria-modal="true"><section className="form-sheet"><button className="close-button" onClick={()=>setOpen(false)} aria-label="Fechar"><X size={18}/></button><p className="eyebrow blue">NOVO AGENDAMENTO</p><h2>Agendar consulta</h2><p>O horário será validado novamente no servidor ao salvar.</p><form action={action} className="record-form"><label>Paciente<select name="patientId" defaultValue="" required><option value="" disabled>Selecione</option>{patients.filter(x=>x.status==="ACTIVE").map(x=><option key={x.id} value={x.id}>{x.fullName}</option>)}</select></label><label>Profissional<select name="professionalId" defaultValue="" required><option value="" disabled>Selecione</option>{professionals.filter(x=>x.status==="ACTIVE").map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Serviço<select name="serviceId" defaultValue="" required><option value="" disabled>Selecione</option>{services.filter(x=>x.status==="ACTIVE").map(x=><option key={x.id} value={x.id}>{x.name} · {x.durationMinutes} min</option>)}</select></label><label>Sala<select name="roomId" defaultValue="" required><option value="" disabled>Selecione</option>{rooms.filter(x=>x.status==="ACTIVE").map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Data<input name="date" type="date" defaultValue={date} required/></label><label>Horário<input name="time" type="time" required/></label>{state.error?<p className="form-error">{state.error}</p>:null}{state.success?<p className="success-message">{state.success}</p>:null}<button className="primary-button full" disabled={pending}>{pending?"Validando horário...":"Confirmar agendamento"}</button></form></section></div>:null}</main> }
