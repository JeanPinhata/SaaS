import { Bell, Bot, CalendarDays, ChartNoAxesCombined, ChevronDown, CircleDollarSign, HeartPulse, LayoutDashboard, MessageCircle, Settings, Stethoscope, UsersRound } from "lucide-react";
import { signOutAction } from "@/app/dashboard/actions";

const navigation = [["Dashboard", "/dashboard", LayoutDashboard], ["Agenda", "/agenda", CalendarDays], ["Pacientes", "/patients", UsersRound], ["Profissionais", "/professionals", Stethoscope], ["Financeiro", "#fase-4", CircleDollarSign], ["WhatsApp / IA", "#fase-6", Bot], ["Mensagens", "#fase-5", MessageCircle], ["Relatórios", "#fase-8", ChartNoAxesCombined], ["Configurações", "#fase-7", Settings]] as const;

export function AppSidebar({ organizationName, userName, active = "Dashboard" }: { organizationName: string; userName: string; active?: string }) {
  return <aside className="sidebar"><div className="sidebar-top"><div className="sidebar-brand"><span className="brand-mark"><HeartPulse size={18} /></span><strong>CliniAI</strong></div><nav aria-label="Navegação principal" className="sidebar-nav">{navigation.map(([label, href, Icon]) => <a href={href} key={label} className={label === active ? "nav-link active" : "nav-link"} aria-current={label === active ? "page" : undefined}><Icon size={17} strokeWidth={1.8} /><span>{label}</span>{label === "Mensagens" ? <span className="nav-badge">3</span> : null}</a>)}</nav></div><div className="sidebar-bottom"><div className="clinic-switcher"><span className="clinic-emblem"><HeartPulse size={15} /></span><span><strong>{organizationName}</strong><small>Plano Pro</small></span><ChevronDown size={15} /></div><div className="profile-row"><span className="avatar">AS</span><span><strong>{userName}</strong><small>Administradora</small></span><form action={signOutAction}><button aria-label="Sair da conta" title="Sair"><Bell size={16} /></button></form></div></div></aside>;
}
